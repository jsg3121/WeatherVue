export type NowStatusRef = {
  nowTemp: number
  nowSky: string
}

export type MinMaxRef = {
  minTemp: number
  maxTemp: number
}

export type SideBarListType = {
  index: number
  name: string
  logo: NodeRequire
  selectLogo: NodeRequire
}
